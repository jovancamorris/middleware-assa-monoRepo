<?xml version="1.0" encoding="UTF-8"?>
<endpoint name="SapCoreDynamicEndpoint" xmlns="http://ws.apache.org/ns/synapse">
    <!-- ================================================================= -->
    <!-- Unified Dynamic Endpoint for SAP Core API Calls                   -->
    <!-- Sesuai CLEAN_ARCHITECTURE.md Bagian 4.4                           -->
    <!-- Operator '+' mencegah double-encoding query parameters (RFC 6570) -->
    <!-- ================================================================= -->
    <http method="GET" uri-template="{+uri.var.sapBackendUrl}">
        <suspendOnFailure>
            <initialDuration>0</initialDuration>
            <progressionFactor>1.0</progressionFactor>
            <maximumDuration>0</maximumDuration>
        </suspendOnFailure>
        <markForSuspension>
            <retriesBeforeSuspension>0</retriesBeforeSuspension>
            <retryDelay>0</retryDelay>
        </markForSuspension>
        <timeout>
            <duration>30000</duration>
            <responseAction>fault</responseAction>
        </timeout>
    </http>
</endpoint>
